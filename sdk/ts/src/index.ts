/**
 * @vanta-protocol/sdk
 *
 * Lightweight re-export of the core SDK client.
 * Install this package if you only need the client without
 * the full protocol internals.
 */

export { VantaClient } from "../../../src/sdk/client";
export type { VantaConfig, VantaStats } from "../../../src/sdk/types";
